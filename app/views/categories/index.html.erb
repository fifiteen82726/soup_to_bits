<h1> Soup Categories</h1>


<ul>
	<% @categories.each do |c| %>
	<li> <%= link_to c.name, c %> (<%= c.soups.count %>) </li> 	

	<% end %> 
	<% if session[:vip] == true %>
	<li> Super BBB</li>
	<% end %>

</ul>